apiVersion: v1
kind: Service
metadata:
  name: {{ include "velox-frontend.fullname" . }}
  labels:
    {{- include "velox-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "velox-frontend.selectorLabels" . | nindent 4 }}
